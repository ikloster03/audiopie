---
sidebar_position: 1
---

# Настройки

AudioPie предоставляет различные настройки для кастомизации вашего рабочего процесса. Откройте настройки, нажав на значок шестерёнки (⚙️) в заголовке.

## Обзор настроек

Диалог настроек содержит следующие опции:

| Настройка | Описание | По умолчанию |
|-----------|----------|--------------|
| Путь FFmpeg | Путь к исполняемому файлу FFmpeg | Встроенный |
| Путь FFprobe | Путь к исполняемому файлу FFprobe | Встроенный |
| Битрейт по умолчанию | Битрейт выходного аудио | 128 кбит/с |
| Папка по умолчанию | Куда сохранять M4B файлы | Домашняя папка |
| Потоки FFmpeg | Потоки CPU для кодирования | Авто |
| Тема | Светлый или тёмный режим | Система |
| Язык | Язык интерфейса | Английский |

## Настройки аудио

### Битрейт по умолчанию

Битрейт влияет на качество аудио и размер файла:

```
┌────────────┬─────────────────┬──────────────────┐
│ Битрейт   │ Качество        │ ~Размер в час    │
├────────────┼─────────────────┼──────────────────┤
│ 64 кбит/с │ Хорошее (голос) │ 28 МБ            │
│ 96 кбит/с │ Лучше           │ 42 МБ            │
│ 128 кбит/с│ Отличное        │ 56 МБ            │
│ 192 кбит/с│ Избыточное      │ 84 МБ            │
└────────────┴─────────────────┴──────────────────┘
```

**Рекомендация**: Для аудиокниг (только голос) 64-96 кбит/с обеспечивает отличное качество при меньшем размере файлов.

### Потоки FFmpeg

Управляет количеством потоков CPU, используемых FFmpeg:

- **Авто (по умолчанию)**: FFmpeg решает на основе вашей системы
- **1-2 потока**: Меньшая нагрузка на CPU, медленнее кодирование
- **4+ потоков**: Быстрее кодирование, выше нагрузка на CPU

Оставьте «Авто», если нет особых требований.

## Настройки вывода

### Папка по умолчанию

Установите папку по умолчанию для сохранения M4B файлов:

1. Нажмите **Обзор** рядом с полем папки по умолчанию
2. Выберите предпочтительную папку
3. Нажмите **Выбрать**

При сборке диалог файлов будет открываться в этом месте.

:::tip
Создайте специальную папку типа `~/Аудиокниги/Выход` для удобной организации.
:::

## Внешний вид

### Тема

Выберите предпочтительную цветовую схему:

- **Светлая** — Яркий интерфейс с тёмным текстом
- **Тёмная** — Тёмный интерфейс со светлым текстом
- **Система** — Следует настройкам ОС

Быстро переключайте тему с помощью значка луны/солнца в заголовке.

### Язык

AudioPie поддерживает:

- 🇬🇧 **English** (Английский)
- 🇷🇺 **Русский**

Измените язык с помощью кнопки языка в заголовке или в Настройках.

## Настройка FFmpeg

Подробности смотрите в специальном руководстве [Настройка FFmpeg](./ffmpeg):

- Использование встроенного FFmpeg
- Использование системного FFmpeg
- Устранение проблем с FFmpeg

## Сохранение настроек

Настройки сохраняются автоматически когда вы:
- Закрываете диалог настроек
- Изменяете любую настройку

Настройки сохраняются между перезапусками приложения.

## Расположение настроек

AudioPie хранит настройки в папке данных пользователя:

| Платформа | Расположение |
|-----------|--------------|
| Linux | `~/.config/audiopie/` |
| Windows | `%APPDATA%\audiopie\` |
| macOS | `~/Library/Application Support/audiopie/` |

## Сброс настроек

Чтобы сбросить все настройки по умолчанию:

1. Закройте AudioPie
2. Удалите файл настроек из указанного выше расположения
3. Перезапустите AudioPie

:::caution
Это сбросит ВСЕ настройки, включая пути FFmpeg и предпочтения.
:::

## Устранение неполадок

### Настройки не сохраняются

- Проверьте права на запись в папку конфигурации
- Попробуйте запустить AudioPie от имени администратора (Windows)
- Проверьте место на диске

### Тема не применяется

- Попробуйте перезапустить AudioPie
- Проверьте, не выбрана ли тема «Система» и не изменилась ли тема ОС
- Очистите кэш приложения

### Язык изменён, но текст не обновился

- Некоторый текст может требовать перезапуска для обновления
- Закройте и снова откройте диалог настроек
- Перезапустите AudioPie

